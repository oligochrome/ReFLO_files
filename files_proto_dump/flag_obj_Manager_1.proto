syntax = "proto3";

package ReFLO;

//  Manager_1
message flag_objFLO {
	uint32	flag_obj_id	= 1;
	uint32	flagId	= 2;
	uint32	objId	= 3;
	int32	objNum	= 4;
}

message tableflag_objFLO { repeated flag_objFLO tableFlagObj = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen flag_obj_Manager_1.proto


			   case "flag_obj.bytes":
					ii = 0;
					tableflag_obj tblManager1 = new tableflag_obj { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager1.TableFlagObj.Add(new flag_objFLO
								{
									FlagObjId = br.ReadUInt32(),
									FlagId = br.ReadUInt32(),
									ObjId = br.ReadUInt32(),
									ObjNum = br.ReadInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager1, Formatting.Indented)); }
					return tblManager1;
*/