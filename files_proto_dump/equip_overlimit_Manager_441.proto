syntax = "proto3";

package ReFLO;

//  Manager_441
message equip_overlimitFLO {
	uint32	equip_overlimit_id	= 1;
	uint32	uniqid	= 2;
	uint32	objId	= 3;
	int32	stage	= 4;
	uint32	vector[0]	= 5;
	uint32	vector[1]	= 6;
	uint32	vector[2]	= 7;
	int32	vector[0]	= 8;
	int32	vector[1]	= 9;
	int32	vector[2]	= 10;
}

message tableequip_overlimitFLO { repeated equip_overlimitFLO tableEquipOverlimit = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen equip_overlimit_Manager_441.proto


			   case "equip_overlimit.bytes":
					ii = 0;
					tableequip_overlimit tblManager441 = new tableequip_overlimit { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager441.TableEquipOverlimit.Add(new equip_overlimitFLO
								{
									EquipOverlimitId = br.ReadUInt32(),
									Uniqid = br.ReadUInt32(),
									ObjId = br.ReadUInt32(),
									Stage = br.ReadInt32(),
									Vector[0] = br.ReadInt32(),
									Vector[1] = br.ReadInt32(),
									Vector[2] = br.ReadInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager441, Formatting.Indented)); }
					return tblManager441;
*/