syntax = "proto3";

package ReFLO;

//  Manager_103
message smile_pointFLO {
	uint32	smile_point_id	= 1;
	uint32	smiletype	= 2;
	int32	point	= 3;		//	Byte
}

message tablesmile_pointFLO { repeated smile_pointFLO tableSmilePoint = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen smile_point_Manager_103.proto


			   case "smile_point.bytes":
					ii = 0;
					tablesmile_point tblManager103 = new tablesmile_point { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager103.TableSmilePoint.Add(new smile_pointFLO
								{
									SmilePointId = br.ReadUInt32(),
									Smiletype = br.ReadUInt32(),
									Point = br.ReadByte()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager103, Formatting.Indented)); }
					return tblManager103;
*/