syntax = "proto3";

package ReFLO;

//  Manager_478
message craft_submissionFLO {
	uint32	craft_submission_id	= 1;
	uint32	Id	= 2;
	uint32	submission_type	= 3;
	uint32	textId	= 4;
	uint32	vector[0]	= 5;
	uint32	vector[1]	= 6;
	int32	vector[0]	= 7;
	int32	vector[1]	= 8;
}

message tablecraft_submissionFLO { repeated craft_submissionFLO tableCraftSubmission = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen craft_submission_Manager_478.proto


			   case "craft_submission.bytes":
					ii = 0;
					tablecraft_submission tblManager478 = new tablecraft_submission { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager478.TableCraftSubmission.Add(new craft_submissionFLO
								{
									CraftSubmissionId = br.ReadUInt32(),
									Id = br.ReadUInt32(),
									SubmissionType = br.ReadUInt32(),
									TextId = br.ReadUInt32(),
									Vector[0] = br.ReadInt32(),
									Vector[1] = br.ReadInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager478, Formatting.Indented)); }
					return tblManager478;
*/