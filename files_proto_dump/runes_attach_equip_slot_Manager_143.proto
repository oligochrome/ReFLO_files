syntax = "proto3";

package ReFLO;

//  Manager_143
message runes_attach_equip_slotFLO {
	uint32	runes_attach_equip_slot_id	= 1;
	uint32	id	= 2;
	int32	equipRarity	= 3;
	uint32	itemTypeL	= 4;
	uint32	itemSlotTypeId	= 5;
	uint32	vector[0]	= 6;
	uint32	vector[1]	= 7;
	uint32	vector[2]	= 8;
	uint32	vector[3]	= 9;
	uint32	vector[4]	= 10;
}

message tablerunes_attach_equip_slotFLO { repeated runes_attach_equip_slotFLO tableRunesAttachEquipSlot = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen runes_attach_equip_slot_Manager_143.proto


			   case "runes_attach_equip_slot.bytes":
					ii = 0;
					tablerunes_attach_equip_slot tblManager143 = new tablerunes_attach_equip_slot { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager143.TableRunesAttachEquipSlot.Add(new runes_attach_equip_slotFLO
								{
									RunesAttachEquipSlotId = br.ReadUInt32(),
									Id = br.ReadUInt32(),
									EquipRarity = br.ReadInt32(),
									ItemTypeL = br.ReadUInt32(),
									ItemSlotTypeId = br.ReadUInt32(),
									Vector[0] = br.ReadUInt32(),
									Vector[1] = br.ReadUInt32(),
									Vector[2] = br.ReadUInt32(),
									Vector[3] = br.ReadUInt32(),
									Vector[4] = br.ReadUInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager143, Formatting.Indented)); }
					return tblManager143;
*/