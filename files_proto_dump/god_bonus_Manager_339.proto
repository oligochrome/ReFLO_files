syntax = "proto3";

package ReFLO;

//  Manager_339
message god_bonusFLO {
	uint32	god_bonus_id	= 1;
	uint32	uniqid	= 2;
	uint32	godBonusId	= 3;
	int32	stage	= 4;
	int32	godRank	= 5;
	uint32	godMissionId	= 6;
	uint32	name	= 7;
	uint32	info	= 8;
	uint32	icon	= 9;
	int32	sort	= 10;
	int32	cost	= 11;
}

message tablegod_bonusFLO { repeated god_bonusFLO tableGodBonus = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen god_bonus_Manager_339.proto


			   case "god_bonus.bytes":
					ii = 0;
					tablegod_bonus tblManager339 = new tablegod_bonus { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager339.TableGodBonus.Add(new god_bonusFLO
								{
									GodBonusId = br.ReadUInt32(),
									Uniqid = br.ReadUInt32(),
									GodBonusId = br.ReadUInt32(),
									Stage = br.ReadInt32(),
									GodRank = br.ReadInt32(),
									GodMissionId = br.ReadUInt32(),
									Name = br.ReadUInt32(),
									Info = br.ReadUInt32(),
									Icon = br.ReadUInt32(),
									Sort = br.ReadInt32(),
									Cost = br.ReadInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager339, Formatting.Indented)); }
					return tblManager339;
*/