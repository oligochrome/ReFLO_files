syntax = "proto3";

package ReFLO;

//  Manager_430
message event_achievementFLO {
	uint32	event_achievement_id	= 1;
	uint32	uniqId	= 2;
	uint32	eventId	= 3;
	uint32	mapEventId	= 4;
}

message tableevent_achievementFLO { repeated event_achievementFLO tableEventAchievement = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen event_achievement_Manager_430.proto


			   case "event_achievement.bytes":
					ii = 0;
					tableevent_achievement tblManager430 = new tableevent_achievement { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager430.TableEventAchievement.Add(new event_achievementFLO
								{
									EventAchievementId = br.ReadUInt32(),
									UniqId = br.ReadUInt32(),
									EventId = br.ReadUInt32(),
									MapEventId = br.ReadUInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager430, Formatting.Indented)); }
					return tblManager430;
*/