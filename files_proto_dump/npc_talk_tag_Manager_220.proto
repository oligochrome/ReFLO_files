syntax = "proto3";

package ReFLO;

//  Manager_220
message npc_talk_tagFLO {
	uint32	npc_talk_tag_id	= 1;
	string	tagStr	= 2;
	uint32	seID	= 3;
	uint32	actID	= 4;
	uint32	effectID	= 5;
}

message tablenpc_talk_tagFLO { repeated npc_talk_tagFLO tableNpcTalkTag = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen npc_talk_tag_Manager_220.proto


			   case "npc_talk_tag.bytes":
					ii = 0;
					tablenpc_talk_tag tblManager220 = new tablenpc_talk_tag { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager220.TableNpcTalkTag.Add(new npc_talk_tagFLO
								{
									NpcTalkTagId = br.ReadUInt32(),
									TagStr = br.ReadString(),
									SeID = br.ReadUInt32(),
									ActID = br.ReadUInt32(),
									EffectID = br.ReadUInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager220, Formatting.Indented)); }
					return tblManager220;
*/