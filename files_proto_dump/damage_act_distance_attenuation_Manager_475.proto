syntax = "proto3";

package ReFLO;

//  Manager_475
message damage_act_distance_attenuationFLO {
	uint32	damage_act_distance_attenuation_id	= 1;
	uint32	uniqId	= 2;
	uint32	objId	= 3;
	uint32	actionId	= 4;
	uint32	missileObjId	= 5;
	float	dampStartDist	= 6;		//	Single
	float	dampEndDist	= 7;		//	Single
	float	dampMinCoef	= 8;		//	Single
}

message tabledamage_act_distance_attenuationFLO { repeated damage_act_distance_attenuationFLO tableDamageActDistanceAttenuation = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen damage_act_distance_attenuation_Manager_475.proto


			   case "damage_act_distance_attenuation.bytes":
					ii = 0;
					tabledamage_act_distance_attenuation tblManager475 = new tabledamage_act_distance_attenuation { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager475.TableDamageActDistanceAttenuation.Add(new damage_act_distance_attenuationFLO
								{
									DamageActDistanceAttenuationId = br.ReadUInt32(),
									UniqId = br.ReadUInt32(),
									ObjId = br.ReadUInt32(),
									ActionId = br.ReadUInt32(),
									MissileObjId = br.ReadUInt32(),
									DampStartDist = br.ReadSingle(),
									DampEndDist = br.ReadSingle(),
									DampMinCoef = br.ReadSingle()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager475, Formatting.Indented)); }
					return tblManager475;
*/