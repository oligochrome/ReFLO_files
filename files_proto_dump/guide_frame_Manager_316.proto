syntax = "proto3";

package ReFLO;

//  Manager_316
message guide_frameFLO {
	uint32	guide_frame_id	= 1;
	uint32	guideTypeId	= 2;
	uint32	id	= 3;
	uint32	textId	= 4;
	uint32	objId	= 5;
	uint32	lifeRank	= 6;
}

message tableguide_frameFLO { repeated guide_frameFLO tableGuideFrame = 1; }

/*
cd E:\OneDrive\FLO\ReFLO\ReFLO\Proto\Def; ..\..\..\packages\Google.Protobuf.Tools.3.19.4\tools\windows_x64\protoc --proto_path=. --csharp_out=..\Gen guide_frame_Manager_316.proto


			   case "guide_frame.bytes":
					ii = 0;
					tableguide_frame tblManager316 = new tableguide_frame { };
					using (MemoryStream sr = new MemoryStream(data))
					{
						using (BinaryReader br = new BinaryReader(sr, Encdg))
						{
							uint tableCount = br.ReadUInt32();
							if (DumpDEBUG) { Console.Error.WriteLine("Expected: {0}", tableCount); }
							while (br.BaseStream.Position != br.BaseStream.Length)
							{
								tblManager316.TableGuideFrame.Add(new guide_frameFLO
								{
									GuideFrameId = br.ReadUInt32(),
									GuideTypeId = br.ReadUInt32(),
									Id = br.ReadUInt32(),
									TextId = br.ReadUInt32(),
									ObjId = br.ReadUInt32(),
									LifeRank = br.ReadUInt32()
								});
								ii += 1;
							}
							if (DumpDEBUG) { Console.Error.WriteLine("Total = {0}", ii); }
							if (tableCount != ii) { Console.Error.WriteLine("ERROR: {0} ENTRY COUNTS DO NOT MATCH! {1} / {2}", baseName, ii, tableCount); }
						}
					}
					if (outputJSON) { File.WriteAllText(outFile, JsonConvert.SerializeObject(tblManager316, Formatting.Indented)); }
					return tblManager316;
*/